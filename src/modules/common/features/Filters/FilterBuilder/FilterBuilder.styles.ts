import styled from 'styled-components'
import { device } from '../../../../../App.styled'

const Wrapper = styled.div`
  width: 100%;

  display: flex;
  align-items: center;
  flex-direction: column;

  gap: 30px;
`

const StyledFilterBuilder = {
  Wrapper,
}

export default StyledFilterBuilder
